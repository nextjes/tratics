export const enum Role {
  Clock = "clock",
  Node = "node",
  Task = "task",
}

export const enum TaskStatus {
  New = "new",
  Runnable = "runnable",
  Running = "running",
  Waiting = "waiting",
  Terminated = "terminated",
}
