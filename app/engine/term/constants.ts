export const enum Role {
  Clock = "clock",
  Node = "node",
}

export const enum TaskStatus {
  New = "new",
  Runnable = "runnable",
  Running = "running",
  Waiting = "waiting",
  Terminated = "terminated",
}
